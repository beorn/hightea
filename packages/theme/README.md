# @silvery/theme

Theming system with semantic color tokens and 38 palettes for silvery (internal package).

> **Note:** This is an internal package. Use `silvery/theme` imports instead.

```console
$ npm install silvery
```

```tsx
import { ThemeProvider, useTheme } from "silvery/theme"
```
