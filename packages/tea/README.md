# @silvery/tea

[TEA (The Elm Architecture)](https://guide.elm-lang.org/architecture/) state machine store for silvery.

Optional structured state management for complex apps — pure `(action, state) -> [state, effects]` with serializable actions and effects.

```console
$ npm install @silvery/tea
```
