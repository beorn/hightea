/**
 * createApp() — re-exported from @silvery/ag-term/runtime (canonical home).
 *
 * This file exists for backward compatibility. The canonical source is
 * `@silvery/ag-term/runtime/create-app`. Import from there for new code.
 */
export { createApp, useApp, useAppShallow, StoreContext } from "@silvery/ag-term/runtime"
export type {
  AppDefinition,
  AppHandle,
  AppRunOptions,
  AppRunner,
  EventHandler,
  EventHandlers,
  EventHandlerContext,
} from "@silvery/ag-term/runtime"
