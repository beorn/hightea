# @silvery/ag-term

Terminal rendering target for silvery (internal package).

> **Note:** This is an internal package. Use [`silvery`](https://github.com/beorn/silvery) instead — it re-exports the public API via `silvery/runtime`.

```console
$ npm install silvery
```
